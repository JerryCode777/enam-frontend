<?xml version="1.0" encoding="utf-8"?>
<!-- Pantalla nativa de arranque: lo que se ve antes de que Flutter dibuje.
     Va en el azul de marca, no en blanco: si no, el usuario ve un destello
     blanco con el logo de Flutter y recién después el splash de la app. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/marca_arranque" />
</layer-list>
